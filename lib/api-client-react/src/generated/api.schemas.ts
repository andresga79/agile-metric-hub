/**
 * Generated by orval v8.13.0 🍺
 * Do not edit manually.
 * Api
 * Agile Metrics Dashboard API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ErrorResponse {
  error: string;
}

export interface SuccessResponse {
  message: string;
}

export interface LoginCredentials {
  /** @minLength 1 */
  username: string;
  /** @minLength 1 */
  password: string;
}

export type UserRoleProperty = typeof UserRoleProperty[keyof typeof UserRoleProperty];


export const UserRoleProperty = {
  admin: 'admin',
  member: 'member',
  viewer: 'viewer',
} as const;

export interface User {
  id: number;
  username: string;
  email: string;
  role: UserRoleProperty;
  createdAt?: string;
}

export interface AuthToken {
  token: string;
  user: User;
}

export type UserRole = typeof UserRole[keyof typeof UserRole];


export const UserRole = {
  admin: 'admin',
  member: 'member',
  viewer: 'viewer',
} as const;

export type AdminUserRole = typeof AdminUserRole[keyof typeof AdminUserRole];


export const AdminUserRole = {
  admin: 'admin',
  member: 'member',
  viewer: 'viewer',
} as const;

export interface AdminUser {
  id: number;
  username: string;
  email: string;
  role: AdminUserRole;
  createdAt?: string;
}

export type AdminCreateUserRequestRole = typeof AdminCreateUserRequestRole[keyof typeof AdminCreateUserRequestRole];


export const AdminCreateUserRequestRole = {
  admin: 'admin',
  member: 'member',
  viewer: 'viewer',
} as const;

export interface AdminCreateUserRequest {
  /** @minLength 1 */
  username: string;
  /** @minLength 1 */
  email: string;
  /** @minLength 1 */
  password: string;
  role?: AdminCreateUserRequestRole;
}

export type AdminUpdateUserRequestRole = typeof AdminUpdateUserRequestRole[keyof typeof AdminUpdateUserRequestRole];


export const AdminUpdateUserRequestRole = {
  admin: 'admin',
  member: 'member',
  viewer: 'viewer',
} as const;

export interface AdminUpdateUserRequest {
  /** @minLength 1 */
  username?: string;
  /** @minLength 1 */
  email?: string;
  /** @minLength 1 */
  password?: string;
  role?: AdminUpdateUserRequestRole;
}

/**
 * Agile board methodology detected from Jira boards
 */
export type ProjectBoardType = typeof ProjectBoardType[keyof typeof ProjectBoardType];


export const ProjectBoardType = {
  scrum: 'scrum',
  kanban: 'kanban',
  simple: 'simple',
} as const;

export interface Project {
  id: string;
  key: string;
  name: string;
  /** @nullable */
  description?: string | null;
  projectType: string;
  /** Agile board methodology detected from Jira boards */
  boardType?: ProjectBoardType;
  /** Human-readable methodology label (Scrum or Kanban) */
  methodology?: string;
  /** @nullable */
  avatarUrl?: string | null;
  issueCount: number;
  doneCount: number;
  inProgressCount: number;
  visible: boolean;
  /** @nullable */
  lead?: string | null;
  /** @nullable */
  url?: string | null;
  /** Whether the data is from Jira or demo/mock data */
  usingMockData?: boolean;
}

export type ProjectWithVisibilityBoardType = typeof ProjectWithVisibilityBoardType[keyof typeof ProjectWithVisibilityBoardType];


export const ProjectWithVisibilityBoardType = {
  scrum: 'scrum',
  kanban: 'kanban',
  simple: 'simple',
} as const;

export interface ProjectWithVisibility {
  id: string;
  key: string;
  name: string;
  /** @nullable */
  description?: string | null;
  projectType: string;
  boardType?: ProjectWithVisibilityBoardType;
  methodology?: string;
  /** @nullable */
  avatarUrl?: string | null;
  issueCount: number;
  doneCount: number;
  inProgressCount: number;
  /** @nullable */
  lead?: string | null;
  /** @nullable */
  url?: string | null;
  usingMockData?: boolean;
  /** Whether this project is visible in the dashboard */
  visible: boolean;
}

export interface ProjectVisibilityUpdate {
  projectId: string;
  visible: boolean;
}

export interface ProjectVisibilityItem {
  projectId: string;
  projectKey: string;
  name: string;
  visible: boolean;
}

export interface ProjectVisibilitySettings {
  projects: ProjectVisibilityItem[];
}

export type UpdateProjectVisibilityRequestProjectsItem = {
  projectKey: string;
  visible: boolean;
};

export interface UpdateProjectVisibilityRequest {
  projects: UpdateProjectVisibilityRequestProjectsItem[];
}

/**
 * Agile board methodology detected from Jira boards
 */
export type ProjectMetricsBoardType = typeof ProjectMetricsBoardType[keyof typeof ProjectMetricsBoardType];


export const ProjectMetricsBoardType = {
  scrum: 'scrum',
  kanban: 'kanban',
  simple: 'simple',
} as const;

export interface WeeklyDataPoint {
  /** Bucket label — calendar week (kanban / non-sprint window) or sprint name (scrum sprint window) */
  label: string;
  value: number;
}

export interface CycleTimeDataPoint {
  range: string;
  count: number;
}

export interface Percentiles {
  /** 50th percentile (median) */
  p50: number;
  /** 75th percentile */
  p75: number;
  /** 85th percentile */
  p85: number;
  /** 95th percentile */
  p95: number;
}

export interface ProjectMetrics {
  projectId: string;
  period: string;
  /** Agile board methodology detected from Jira boards */
  boardType: ProjectMetricsBoardType;
  /** Whether velocity applies (only true for Scrum projects) */
  isScrum: boolean;
  /**
     * Story points completed per sprint (null for non-Scrum projects)
     * @nullable
     */
  velocity: number | null;
  /** Average days from start to done */
  cycleTime: number;
  /** Average days from creation to done (lead time) */
  leadTime: number;
  /** Issues completed per week */
  throughput: number;
  /** Total resolved issues in period */
  resolvedCount: number;
  /** Percentage change vs previous period */
  velocityTrend: number;
  /** Percentage change vs previous period */
  throughputTrend: number;
  velocityByWeek: WeeklyDataPoint[];
  cycleTimeDistribution?: CycleTimeDataPoint[];
  /** P50/P75/P85/P95 cycle time percentiles */
  cycleTimePercentiles: Percentiles;
  /** P50/P75/P85/P95 lead time percentiles */
  leadTimePercentiles: Percentiles;
}

export interface MemberStats {
  accountId: string;
  displayName: string;
  /** @nullable */
  avatarUrl?: string | null;
  issuesResolved: number;
  /** Of issuesResolved, how many carried a story point estimate — bugs/support work often don't, so storyPoints alone understates their contribution. */
  issuesResolvedWithPoints: number;
  storyPoints: number;
  /** Mean time from first "in progress" transition to resolution (not from creation — that's avgLeadTime). */
  avgCycleTime: number;
  /** Mean time from creation to resolution, including backlog wait time. */
  avgLeadTime: number;
  /** Currently open issues assigned to this member, regardless of when they were created. */
  issuesInProgress: number;
  /** Of issuesInProgress, how many are blocked right now (status or Flagged field). */
  issuesBlocked: number;
}

export interface Issue {
  id: string;
  key: string;
  summary: string;
  status: string;
  issueType: string;
  priority: string;
  /** @nullable */
  assignee?: string | null;
  /**
     * Jira account ID — join key against MemberStats.accountId. Prefer this over the display name, which isn't guaranteed unique across an org.
     * @nullable
     */
  assigneeAccountId?: string | null;
  /** Jira's own statusCategory === "indeterminate" — the same signal MemberStats.issuesInProgress counts against. Don't re-derive "in progress" from the status name string; category names vary a lot per project/workflow. */
  isInProgress: boolean;
  /** @nullable */
  storyPoints?: number | null;
  createdAt: string;
  /** @nullable */
  resolvedAt?: string | null;
  /** @nullable */
  cycleTimeDays?: number | null;
}

export interface CfdDataPoint {
  date: string;
  /** Issues in To Do / backlog state */
  todo: number;
  /** Issues actively being worked on */
  inProgress: number;
  /** Issues completed */
  done: number;
}

export interface CfdResponse {
  projectId: string;
  period: string;
  dataPoints: CfdDataPoint[];
}

export interface DashboardSummary {
  avgCycleTime: number;
  /** Whether the data is from Jira or demo/mock data */
  usingMockData?: boolean;
}

export type ForecastRequestUnit = typeof ForecastRequestUnit[keyof typeof ForecastRequestUnit];


export const ForecastRequestUnit = {
  issues: 'issues',
  story_points: 'story_points',
} as const;

export interface ForecastRequest {
  /** Number of issues or story points to forecast */
  target: number;
  unit?: ForecastRequestUnit;
  /** Number of Monte Carlo simulations (max 50000) */
  simulations?: number;
  /** Historical lookback window in days (30, 90, or 180) */
  windowDays?: number;
}

export type ForecastResponseUnit = typeof ForecastResponseUnit[keyof typeof ForecastResponseUnit];


export const ForecastResponseUnit = {
  issues: 'issues',
  story_points: 'story_points',
} as const;

export interface ForecastHistogramBin {
  week: number;
  /** Percentage of simulations */
  count: number;
}

export interface ForecastResponse {
  target: number;
  unit: ForecastResponseUnit;
  simulations: number;
  /** Probability of hitting target (0-100%) */
  probability: number;
  medianWeeks: number;
  p75Weeks: number;
  p85Weeks: number;
  p95Weeks: number;
  histogram: ForecastHistogramBin[];
}

export interface HealthDimension {
  name: string;
  /** Normalized score 0-100 */
  value: number;
  description: string;
}

export interface HealthResponse {
  projectId: string;
  period: string;
  dimensions: HealthDimension[];
}

export interface RejectedBySprint {
  sprintName: string;
  /** QA→Dev reversions in this sprint */
  rejectedCount: number;
  /** Times issues entered QA in this sprint */
  totalEnteredQa: number;
  /** Rejection rate percentage */
  rate: number;
  /** Linked bugs created during this sprint */
  bugsCount: number;
  /** Bugs per issue that entered QA */
  bugRate: number;
  /** Combined rejection + bug impact rate */
  qaImpactRate: number;
}

export interface RejectedIssue {
  key: string;
  summary: string;
  issueType: string;
  fromStatus: string;
  toStatus: string;
  transitionedAt: string;
  /** @nullable */
  sprintName?: string | null;
}

export interface LinkedBug {
  bugKey: string;
  bugSummary: string;
  bugStatus: string;
  bugCreated: string;
  parentStoryKey: string;
  parentStorySummary: string;
  /** Whether the parent story was rejected by QA */
  parentStoryRejected: boolean;
}

export interface QaRejectedResponse {
  projectId: string;
  period: string;
  /** Auto-detected QA status names */
  qaStatuses: string[];
  /** Auto-detected Dev/backlog status names (not QA, not Done) */
  devReturnStatuses: string[];
  /** Total QA→Dev transitions across all issues */
  totalRejectedReversions: number;
  /** Unique issues with at least one QA rejection */
  totalIssuesRejected: number;
  /** Unique issues that ever entered a QA status */
  totalIssuesThatEnteredQa: number;
  /** Percentage of issues rejected by QA out of those that entered QA */
  overallRejectionRate: number;
  /** Bugs linked to stories via issuelinks */
  totalLinkedBugs: number;
  /** Bug issues not linked to any story */
  totalStandaloneBugs: number;
  /** Bugs per issue that entered QA */
  overallBugRate: number;
  /** Combined rejection + bug impact rate */
  overallQaImpactRate: number;
  bySprint: RejectedBySprint[];
  rejectedIssues: RejectedIssue[];
  linkedBugs: LinkedBug[];
}

/**
 * Which base completionRate was computed on — falls back to issue count when the sprint has no story points logged. Two sprints' rates aren't comparable unless the basis matches.
 */
export type SprintMetricCompletionBasis = typeof SprintMetricCompletionBasis[keyof typeof SprintMetricCompletionBasis];


export const SprintMetricCompletionBasis = {
  storyPoints: 'storyPoints',
  issueCount: 'issueCount',
} as const;

export interface IssueTypeBreakdown {
  Story: number;
  Bug: number;
  Task: number;
  Epic: number;
  Other: number;
}

export interface SprintMetric {
  sprintId: number;
  sprintName: string;
  state: string;
  /** @nullable */
  startDate: string | null;
  /** @nullable */
  endDate: string | null;
  totalIssues: number;
  totalStoryPoints: number;
  completedIssues: number;
  completedStoryPoints: number;
  /** Percentage of issues completed (0-100) */
  completionRate: number;
  /** Which base completionRate was computed on — falls back to issue count when the sprint has no story points logged. Two sprints' rates aren't comparable unless the basis matches. */
  completionBasis: SprintMetricCompletionBasis;
  /** Story points completed in this sprint */
  velocity: number;
  /** Number of issues reopened after being marked done */
  reopenedCount: number;
  /** @nullable */
  avgCycleTimeDays: number | null;
  breakdown: IssueTypeBreakdown;
  /** Number of issues in this sprint that were already assigned to an earlier sprint (per Jira's Sprint field history) whose end date precedes this sprint's start date — work that slipped from a previous sprint into this one. */
  carryoverCount: number;
  /** Story points belonging to carryoverCount issues. */
  carryoverStoryPoints: number;
  /** Percentage of this sprint's total story points that is carryover (0-100). */
  carryoverRate: number;
}

export interface SprintMetricsSummary {
  /** Closed sprints the averages below are computed from. An active sprint (if any) is included in the top-level `sprints` array but excluded here — it's still in progress, so folding its partial numbers into these averages would misrepresent them as a decline. */
  totalSprints: number;
  avgVelocity: number;
  avgCompletionRate: number;
  /** @nullable */
  avgCycleTimeDays: number | null;
  totalCompletedStoryPoints: number;
  /** How many sprints actually fall within the selected period, before capping to the display limit. Higher than `sprints.length` means older sprints were cut off. */
  totalSprintsInPeriod: number;
}

export interface SprintMetricsResponse {
  sprints: SprintMetric[];
  summary: SprintMetricsSummary;
}

export interface WeekMetric {
  /** ISO date of the Monday of this week */
  weekStart: string;
  /** Human-readable label like "Mar 2 - Mar 8" */
  weekLabel: string;
  /** Issues completed this week (throughput) */
  totalIssues: number;
  totalStoryPoints: number;
  /** @nullable */
  avgCycleTimeDays: number | null;
  reopenedCount: number;
  breakdown: IssueTypeBreakdown;
}

export interface KanbanMetricsSummary {
  totalWeeks: number;
  avgThroughput: number;
  /** @nullable */
  avgCycleTimeDays: number | null;
  totalCompletedIssues: number;
  totalCompletedStoryPoints: number;
}

export interface KanbanMetricsResponse {
  weeks: WeekMetric[];
  summary: KanbanMetricsSummary;
}

/**
 * How `periods` is bucketed - by sprint for Scrum projects, by ISO week for Kanban
 */
export type EvolutionResponseGranularity = typeof EvolutionResponseGranularity[keyof typeof EvolutionResponseGranularity];


export const EvolutionResponseGranularity = {
  sprint: 'sprint',
  week: 'week',
} as const;

export interface EvolutionPeriod {
  /** Display label - short sprint label (e.g. "SP106") or ISO week number (e.g. "S22") */
  label: string;
  /**
     * Full date range for weekly (Kanban) rows, e.g. "Ago 10 - Ago 16" - same phrasing as the Kanban Semanal breakdown table. Null for sprint rows, whose full name/dates aren't a fixed calendar range.
     * @nullable
     */
  rangeLabel: string | null;
  /** ISO date this period starts (sprint start date, or the Monday of the week) */
  start: string;
  /** True for the currently in-progress sprint (always false for weekly/Kanban rows) - the UI renders it distinctly since its metrics are still climbing, not final */
  isActive: boolean;
  /** @nullable */
  leadTimeAvg: number | null;
  /** @nullable */
  cycleTimeAvg: number | null;
  throughput: number;
  /**
     * Percentage of issues entering QA that were rejected back, or null if none entered QA in this period
     * @nullable
     */
  qaRejectionRate: number | null;
}

export interface EvolutionTargets {
  /** @nullable */
  leadTime: number | null;
  /** @nullable */
  cycleTime: number | null;
  /** @nullable */
  throughput: number | null;
}

export interface EvolutionResponse {
  projectId: string;
  /** How `periods` is bucketed - by sprint for Scrum projects, by ISO week for Kanban */
  granularity: EvolutionResponseGranularity;
  periods: EvolutionPeriod[];
  targets: EvolutionTargets;
}

